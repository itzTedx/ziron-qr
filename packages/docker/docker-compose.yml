name: 'ziron-qr'

services:
  postgres:
    image: postgres:17.0
    container_name: postgres-ziron-qr
    hostname: ${DB_HOST}
    ports:
      - '5432:${DB_PORT}'
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - pg-data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7.2-alpine
    container_name: redis-ziron-qr
    ports:
      - '6379:${REDIS_PORT:-6379}'
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  redis-data:
  pg-data:
